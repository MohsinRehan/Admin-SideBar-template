import React from 'react'
import SideBar from '../components/Sidebar/SideBar'

const FileManager = () => {
  return (
    <SideBar>
      <h1>FileManager</h1>
    </SideBar>
  )
}

export default FileManager
